# Build scratch and local notes.
# Note: the site's own images are NOT scratch — they must be committed, or the
# published page loses its icons, its installability and its link preview.
*.png
!assets/*.png

*.pdf
*.log
__pycache__/
.venv/
.DS_Store
Thumbs.db

# The built file IS committed — it is what you open, and what Pages serves.
# Comment the next two lines out if you would rather build it yourself.
#index.html
#sw.js
